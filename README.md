# Ruler

Install

```bash
# (Recommended) Create a new conda environment
conda create -n RULER python=3.12 -y
conda activate RULER

# Install dependencies
pip install Cython
pip install -r requirements_no_version.txt
python3 ./download_nltk.py
```

Run

```bash
./foo.sh
```